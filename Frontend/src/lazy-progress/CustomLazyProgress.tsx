import { Box, LinearProgress } from "@mui/material";

const CustomLazyProgress = () => {
  return (
    <Box sx={{ width: "100%" }}>
      <LinearProgress />
    </Box>
  );
};

export default CustomLazyProgress;
